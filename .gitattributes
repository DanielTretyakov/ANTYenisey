# По умолчанию git на Windows забирает файлы с CRLF. Для большей части
# репозитория это безобидно, но не для всего.
* text=auto

# Скрипты и SQL остаются с LF при любой настройке core.autocrlf. Причина
# прикладная: разворачиваться будем на Linux-VPS, а bash-скрипт с CRLF там
# падает с «bad interpreter: /usr/bin/env bash^M» — ошибкой, по тексту
# которой причину не угадать.
*.sh text eol=lf
*.sql text eol=lf
*.mjs text eol=lf

# Lock-файл трогать не нужно вовсе: его пишет и читает только pnpm.
pnpm-lock.yaml -diff linguist-generated

# Скиллы Claude Code вендорятся в репозиторий целиком. Их контрольные суммы
# считаются по LF-байтам, поэтому core.autocrlf=true на Windows превращает
# свежий чекаут в «stale snapshot» — скилл молча не грузится. Правило
# дублирует .claude/skills/ui-ux-pro-max/data/.gitattributes, но на весь
# каталог: там же лежат SKILL.md, python-скрипты и csv-каталоги.
.claude/skills/** text eol=lf
.claude/skills/**/*.ttf binary
