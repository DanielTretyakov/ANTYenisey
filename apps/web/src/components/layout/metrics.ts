/**
 * Геометрия шапки — одна на все её воплощения.
 *
 * Высота фиксированная и одна на все разрешения: не `h-20 sm:h-24`. Полоса,
 * меняющая рост при смене ширины окна или при входе в аккаунт, читается как
 * сломанная вёрстка, а не как отзывчивость.
 *
 * Отдельным модулем, а не константой в SiteHeader: та помечена `use client`, и
 * серверному AuthLayout пришлось бы тянуть весь её клиентский код ради одной
 * строки.
 */
export const HEADER_HEIGHT = 'h-20';

/** Высота логотипа в шапке, rem. Одинаковая везде — см. HEADER_HEIGHT. */
export const HEADER_LOGO_HEIGHT = 1.625;
