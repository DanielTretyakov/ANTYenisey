/**
 * Клуб, который обслуживает этот экземпляр веб-приложения.
 *
 * Человек код клуба не вводит и не видит — он видит официальное название,
 * которое подгружается из API по этому коду. Пока платформу обслуживает один
 * клуб, значение берётся из окружения; когда клубов станет несколько, код
 * будет приходить из адреса (поддомен или сегмент пути), а не отсюда.
 */
export const TENANT_SLUG = process.env.NEXT_PUBLIC_TENANT_SLUG ?? 'yenisey';
