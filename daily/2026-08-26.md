# Daily Log: 2026-08-26

## Sessions

## Memory Maintenance

### Memory Flush (12:24)

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (12:26)

FLUSH_OK - Nothing worth saving from this session

### Session (12:30)

**Context:** Завершение настройки скиллов и MCP 21st.dev для проекта «Енисей»

**Key Exchanges:**
- ui-ux-pro-max: 130 тестов / 7923 подтеста — зелено; добавлен `data/.gitattributes` с `* text eol=lf` для защиты от повторного CRLF
- Три плагина установлены (user scope): marketing-skills (50 скиллов), taste-skill (13), karpathy-skills (1) — итого 64 новых скилла
- MCP 21st.dev настроен как HTTP-сервер в `.mcp.json` с подстановкой `${API_KEY_21ST}` — ключ не в репо
- Ключ 21st.dev получен, сохранён через `setx`, подключение проверено: `21st: ✓ Connected`, 21 инструмент доступен
- `get_inspiration` умеет учитывать design context — потенциально полезен вместе с `apps/web/public/brand/`

**Decisions Made:**
- MCP 21st.dev подключён как `"type": "http"` напрямую к `https://21st.dev/api/mcp`, а не через устаревший npx `@21st-dev/magic`
- Ключ хранится в переменной окружения Windows (`setx`), в `.mcp.json` — только `${API_KEY_21ST}`

**Lessons Learned:**
- `setx` не влияет на уже запущенные процессы; `Developer: Reload Window` в VSCode тоже не помогает — нужен полный перезапуск VSCode (`File → Exit`), чтобы новые env vars подхватились
- Терминал, из которого запускаешь `code .`, тоже должен быть открыт после `setx`, иначе VSCode унаследует старое окружение
- Пользователь прислал API-ключ открытым текстом в чат — напомнили о риске, предложили перевыпустить

**Action Items:**
- Перезапустить VSCode полностью (не Reload Window), чтобы 21st.dev и 64 скилла заработали
- После перезапуска проверить `/mcp` (21st connected) и `/plugin` (три плагина видны)
- Рассмотреть перевыпуск API-ключа 21st.dev, т.к. он засветился в истории чата

